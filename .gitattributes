# Normalize line endings to LF
* text eol=lf

# Normalize line endings of Windows files to CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# Mark binary files that are confused as text as not text
*.sqlite3 -text -diff
